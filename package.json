{
  "name": "root",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "latest",
    "@commitlint/config-conventional": "latest",
    "lerna": "^6.0.1",
    "husky": "latest"
  },
  "scripts": {
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint"
    ]
  }
}
